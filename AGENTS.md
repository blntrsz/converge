Use npm run typecheck for type checking.

Check ./CONTEXT.md for terminology questions.

# Packages to explore

You can explore dependencies that is added as git submodules in .agents/. (e.g. effect library in @.agents/effect-smol)

## Agent skills

### Issue tracker

Issues and planning artifacts use GitHub Issues; external PRs are not a triage request surface. See `docs/agents/issue-tracker.md`.

### Triage labels

The tracker uses the default mattpocock/skills label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository using root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
