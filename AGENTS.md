Use npm run typecheck for type checking.

Check ./CONTEXT.md for terminology questions.

# Packages to explore

You can explore dependencies that is added as git submodules in .agents/. (e.g. effect library in @.agents/effect-smol)

## Agent skills

### Issue tracker

Linear — **Converge** project via GraphQL API (`LINEAR_API_KEY`). See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical five-role vocabulary; `ready-for-agent` and `wontfix` already exist on the repo. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` at repo root, ADRs in `docs/adr/`. See `docs/agents/domain.md`.
