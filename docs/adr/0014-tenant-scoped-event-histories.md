# Tenant-Scoped Event Histories

Converge supports single-tenant and multi-tenant modes. A single-tenant application has one Event History, while a multi-tenant application has one Event History per tenant. This makes Event History scope explicit and avoids mixing unrelated tenants into one authoritative sequence.
