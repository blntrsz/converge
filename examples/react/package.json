{
  "name": "@converge/react-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "api",
    "core",
    "ui"
  ],
  "scripts": {
    "dev": "bun run --parallel --filter @converge/react-api --filter @converge/react-ui dev",
    "start": "bun run --parallel --filter @converge/react-api --filter @converge/react-ui start",
    "build": "bun run --filter @converge/react-ui build",
    "typecheck": "bun run --workspaces --if-present --sequential typecheck"
  }
}
